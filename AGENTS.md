# Market Note agent entrypoint

이 저장소에서 작업하는 모든 코딩 에이전트와 새 터미널 세션은 먼저 아래 순서로 읽는다.

1. `PROJECT_STATE.md` — 현재 구현·검증·배포 상태
2. `HARNESS.md` — 하네스 선택과 실행법
3. 작업에 해당하는 `harnesses/*.md`
4. `config/analysis-policy.json` — 분석 품질과 데이터 정직성 규칙

## 제품 목표

초보 투자자가 매일 시장을 공부하면서 스스로 판단하는 능력을 기르는 웹앱이다. 뉴스 요약이나 당일 등락률 순위가 아니라, 2년 구조·1년 추세·3개월 가속도·최근 사건을 연결한다.

모든 분석은 `기대 형성 → 실제 결과 → 실망/재평가 → 가격·거래량 반응 → 다음 확인 조건` 구조를 따른다. 사실, 해석, 가설을 구분하고 근거 링크와 데이터 시점을 표시한다.

## 절대 규칙

- `.env`, API 키, Telegram 토큰, 개인정보를 출력·커밋·로그에 남기지 않는다.
- 확인되지 않은 컨센서스, 목표주가, 원인을 사실처럼 쓰지 않는다.
- 가격 동행만으로 인과관계를 단정하지 않는다.
- 유료 리포트 원문을 우회 수집하거나 재배포하지 않는다.
- GitHub Pages는 정적 프론트엔드만 실행한다. Python API는 로컬 또는 별도 호스팅이 필요하다.
- 사용자 작업을 덮어쓰지 않고 작은 검증 가능한 커밋으로 진행한다.

## 기본 검증

```bash
./scripts/harness check
```

